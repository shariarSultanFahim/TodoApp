import useDocumentTitle from "../../CustomHooks/useDocumentTitle";

const Error = () => {
    useDocumentTitle('Error')
    return (
        <div>
            <h1>Error</h1>
        </div>
    );
};

export default Error;